interface:
  display_name: "Agent Orchestrator"
  short_description: "Repository-aware, token-aware multi-agent orchestration"
  default_prompt: "Inspect the repository, protect existing changes, classify the work, delegate only safe independent workstreams, audit changed paths, then integrate and verify."
policy:
  allow_implicit_invocation: false
