interface:
  display_name: "Agent Orchestrator"
  short_description: "Repository-aware multi-agent development orchestration"
  default_prompt: "Inspect the repository, classify the work, delegate only safe independent workstreams, review results, then integrate and verify."
policy:
  allow_implicit_invocation: true
